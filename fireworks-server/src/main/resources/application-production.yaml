server:
  name: fireworks-server
  port: 80

logging:
  config: classpath:log4j2-prod.xml

wechat:
  enabled: false
  app:
    id: <appid>
    secret: <secret>
  authentication:
    token: <token>
    encodingAESKey: <encodingAESKey>